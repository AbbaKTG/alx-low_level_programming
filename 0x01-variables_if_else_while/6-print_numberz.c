#include <stdio.h>
/**
 * main - main function
 *
 * Return: 0 if success
 */

int main(void)
{
        for (int i = 0; i < 10; i++)
                putchar(i + '0');
        putchar('\n')
        return (0);
}
