#include <stdio.h>
/**
 * main - main function
 *
 * Return: 0 if success
 */

int main(void)
{
        for (char = 'z'; i >= 'a'; i--)
            putchar(i);
        putchar('\n');
        return (0);
}
